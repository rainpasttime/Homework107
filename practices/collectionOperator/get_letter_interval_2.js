'use strict';

function get_letter_interval_2(number_a, number_b) {
  var result = [];
  var s = "a";
  if(number_a<number_b){
      for(var i =number_a;number_a<=number_b;number_a++)
      {
          var p = "a";
          var tem  = number_a;
          while(tem>0)
          {
              if()
          }
      }
  }

  return result;
}

module.exports = get_letter_interval_2;

